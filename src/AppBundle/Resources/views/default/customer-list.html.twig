{% for customer in customers %}
    <a class="list-group-item d-flex justify-content-between align-items-center list-group-item-action" href="{{ path('edit_customer', { 'id': customer.id }) }}">
        {{ customer.getFullName() }}
    </a>
{% endfor %}